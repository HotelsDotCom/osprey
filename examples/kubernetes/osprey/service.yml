apiVersion: v1
kind: Service
metadata:
  name: @ospreyService@
  namespace: @namespace@
spec:
  ports:
  - name: osprey
    protocol: TCP
    port: @ospreyPort@
    targetPort: @ospreyPort@
  selector:
    app: @ospreyApp@
  type: ClusterIP
